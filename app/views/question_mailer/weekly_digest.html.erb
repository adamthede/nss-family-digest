<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p>
      <h1>Question and Answer Digest:</h1>
      <h2><%= @group.name %></h2>
      <h4><%= @question %></h4>
      <% if @question_record.created_at %>
        <h4><%= @question_record.created_at.strftime("%B %d, %Y") %></h4>
      <% end %>
    </p>
    <table>
      <tbody>
        <% @group.users.each do |user| %>
          <tr>
            <% if user.profile_image_url(:thumb) %>
              <td><%= image_tag user.profile_image_url(:thumb).to_s %></td>
            <% else %>
              <td><%= image_tag user.gravatar_url %></td>
            <% end %>
            <td><%= user.email %></td>
            <% @answers.each do |answer| %>
              <% if user.id == answer.user_id %>
                <td><%= answer.answer %></td>
              <% end %>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>