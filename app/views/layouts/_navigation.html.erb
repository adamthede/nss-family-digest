<div class="sticky top-0 z-50" data-controller="mobile-menu">
  <nav class="bg-gradient-to-r from-blue-500 to-indigo-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <!-- Logo/Brand -->
        <div class="flex-shrink-0">
          <%= link_to root_path, class: "flex items-center space-x-3 group" do %>
            <span class="text-2xl font-bold text-white group-hover:text-indigo-200 transition-colors duration-200">A2A</span>
            <span class="hidden md:block text-indigo-100 group-hover:text-white transition-colors duration-200">Answers 2 Answers</span>
          <% end %>
        </div>

        <!-- Desktop Navigation -->
        <div class="hidden md:block">
          <div class="ml-4 flex items-center space-x-4">
            <%= render 'layouts/navigation_links' %>
            <% if current_user&.global_admin? %>
              <div class="relative group">
                <%= link_to admin_dashboard_path,
                    class: "px-3 py-2 text-sm font-medium text-indigo-100 hover:text-white transition-colors duration-200" do %>
                  <span>Admin</span>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>

        <!-- Mobile Menu Button -->
        <div class="md:hidden">
          <button
            id="mobile-menu-button"
            class="inline-flex items-center justify-center p-2 rounded-md text-indigo-100 hover:text-white hover:bg-indigo-500 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white transition-all duration-200"
            aria-expanded="false"
            data-action="click->mobile-menu#toggle"
          >
            <span class="sr-only">Open main menu</span>
            <svg id="menu-icon-open" class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" data-mobile-menu-target="openIcon">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg id="menu-icon-close" class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" data-mobile-menu-target="closeIcon">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden" data-mobile-menu-target="menu">
      <div class="px-2 pt-2 pb-3 space-y-1 bg-indigo-600">
        <%= render 'layouts/navigation_links_mobile' %>
        <% if current_user&.global_admin? %>
          <%= link_to admin_dashboard_path,
              class: "text-indigo-100 hover:text-white hover:bg-indigo-500 block px-3 py-2 rounded-md text-base font-medium transition-all duration-200" do %>
            <i class="fas fa-shield-alt mr-1"></i>
            Admin
          <% end %>
        <% end %>
      </div>
    </div>
  </nav>
</div>