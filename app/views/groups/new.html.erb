<div class="container mx-auto px-4 py-8">
  <% if notice.present? %>
    <div id="notice" class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4">
      <%= notice %>
    </div>
  <% end %>

  <h1 class="text-2xl font-bold mb-6">New Group</h1>

  <%= render 'form' %>

  <div class="mt-4">
    <%= link_to 'Back', groups_path, class: "text-blue-600 hover:text-blue-800" %>
  </div>
</div>