<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .header h1 {
      color: #4f46e5;
      margin-bottom: 10px;
    }
    .header p {
      color: #6b7280;
      font-size: 16px;
    }
    .question-card {
      background-color: #f9fafb;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 20px;
      border-left: 4px solid #4f46e5;
    }
    .question-content {
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 10px;
    }
    .question-meta {
      color: #6b7280;
      font-size: 14px;
      margin-bottom: 15px;
    }
    .answers-section {
      margin-top: 15px;
    }
    .answer-card {
      background-color: #ffffff;
      border-radius: 6px;
      padding: 15px;
      margin-bottom: 10px;
      border: 1px solid #e5e7eb;
    }
    .answer-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
    }
    .answer-author {
      font-weight: bold;
      color: #4b5563;
    }
    .answer-date {
      color: #9ca3af;
      font-size: 12px;
    }
    .answer-content {
      color: #1f2937;
    }
    .footer {
      margin-top: 30px;
      text-align: center;
      color: #6b7280;
      font-size: 14px;
      border-top: 1px solid #e5e7eb;
      padding-top: 20px;
    }
    .button {
      display: inline-block;
      background-color: #4f46e5;
      color: white;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 4px;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= @group.name %> - Weekly Digest</h1>
    <p>Questions from <%= @digest.start_date.strftime("%b %d, %Y") %> to <%= @digest.end_date.strftime("%b %d, %Y") %></p>
  </div>

  <p>Hello <%= @user.name %>,</p>

  <p>Here's a summary of the questions and answers shared in your group this week:</p>

  <% if @question_records.empty? %>
    <p>No questions were sent during this period.</p>
  <% else %>
    <% @question_records.each do |record| %>
      <div class="question-card">
        <div class="question-content"><%= record.question.content %></div>
        <div class="question-meta">Sent on <%= record.created_at.strftime("%b %d, %Y") %></div>

        <% if record.answers.any? %>
          <div class="answers-section">
            <h3>Answers:</h3>
            <% record.answers.each do |answer| %>
              <div class="answer-card">
                <div class="answer-header">
                  <span class="answer-author"><%= answer.user.name %></span>
                  <span class="answer-date"><%= answer.created_at.strftime("%b %d, %Y at %I:%M %p") %></span>
                </div>
                <div class="answer-content"><%= answer.content %></div>
              </div>
            <% end %>
          </div>
        <% else %>
          <p>No answers yet for this question.</p>
        <% end %>
      </div>
    <% end %>
  <% end %>

  <p>
    <a href="<%= group_url(@group) %>" class="button">View Group</a>
  </p>

  <div class="footer">
    <p>You're receiving this email because you're a member of <%= @group.name %>.</p>
    <p>If you no longer wish to receive these digests, you can leave the group or contact the group leader.</p>
  </div>
</body>
</html>